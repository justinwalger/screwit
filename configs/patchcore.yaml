# PatchCore hyperparameters. Loaded when MODEL_NAME=patchcore (see .env.example).
train_batch_size: 32
eval_batch_size: 32
num_neighbors: 6
coreset_sampling_ratio: 0.5
# No-op: PatchCore forces max_epochs=1 internally regardless of this value.
max_epochs: 10
